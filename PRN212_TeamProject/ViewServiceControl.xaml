<UserControl x:Class="PRN212_TeamProject.ViewServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PRN212_TeamProject"
             mc:Ignorable="d">
    <StackPanel Margin="0,10,0,0">
        <Grid Margin="0,0,0,10">
            <TextBox x:Name="txtSearch" Height="20" Width="220"  HorizontalAlignment="Left" Margin="180,0,0,0" />
            <Button Height="20" Width="60" Margin="380,0,0,0" Content="Search" Click="btnSearch_Click"/>
        </Grid>
        <DataGrid x:Name="dgService"
                  AutoGenerateColumns="False"
                  Height="210"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ServiceId}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
