<UserControl x:Class="PRN212_TeamProject.BookingTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <Grid>
        <DataGrid x:Name="dgBooking"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Margin="10"
                  HorizontalScrollBarVisibility="Visible"
                  SelectionChanged="dgBooking_SelectedChanged"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookingId}" Width=".3*"/>
                <DataGridTextColumn Header="Patient" Binding="{Binding Patient.Name}" Width="*"/>
                <DataGridTextColumn Header="Doctor" Binding="{Binding Slot.Shift.Doctor.DoctorName}" Width="*"/>
                <DataGridTextColumn Header="Service" Binding="{Binding Service.Name}" Width="*"/>
                <DataGridTextColumn Header="Date" Binding="{Binding DateBooking}" Width="*"/>
                <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
